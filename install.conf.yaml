- defaults:
    link:
      create: true
      relink: true
- shell:
  - [git submodule update --init --recursive, Installing submodules]

# zsh related
- link:
    ~/.zshrc:
    ~/.zsh:
    ~/.oh-my-zsh: ohmyzsh
    ~/.oh-my-zsh/custom/plugins: 
        path: zsh/plugins
        force: true
