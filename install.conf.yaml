- defaults:
    link:
      create: true
      relink: true
- link:
    ~/.shell:
    ~/.zshrc:
    ~/.zsh:
    ~/.oh-my-zsh: ohmyzsh
- shell:
  - [git submodule update --init --recursive, Installing submodules]
