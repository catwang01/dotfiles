- defaults:
    link:
      create: true
      relink: true
- link:
    ~/.shell:
    ~/.zshrc:
    ~/.zsh:
    ~/.on-my-zh:
- shell:
  - [git submodule update --init --recursive, Installing submodules]
